- 采用 React Router v7 进行集中式路由编排，利用嵌套路由将 `Layout` 组件作为受保护业务页面的统一外壳。
- 通过 `RequireAuth` 高阶组件结合 `authStore` 实现全局身份校验，未登录用户强制重定向至 `/login`。
- 针对赞助与致谢等社区页面，设计了基于主机名的 `ExternalRedirect` 机制，在本地开发环境与线上生产环境间自动切换渲染策略。
- 以 `App.tsx` 为顶层入口，协调 Zustand 状态持久化（Hydration）与 Vercel 性能监控的初始化。
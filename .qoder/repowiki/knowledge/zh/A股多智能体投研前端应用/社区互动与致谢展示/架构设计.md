- 采用无状态 React 函数组件架构，直接依赖 `react-router-dom` 进行导航，通过 `lucide-react` 实现视觉表达。
- 数据层通过 `@/services/api` 封装的 RESTful 客户端与后端交互（如 `/v1/sponsors`、`/v1/feedbacks`），并在 `Thanks.tsx` 中结合 GitHub API 实现多源数据聚合。
- 内部实现了基于 `localStorage` 的贡献者数据缓存机制（10分钟 TTL）以及反馈模块的列表/详情视图切换逻辑，形成了轻量级的本地状态管理闭环。
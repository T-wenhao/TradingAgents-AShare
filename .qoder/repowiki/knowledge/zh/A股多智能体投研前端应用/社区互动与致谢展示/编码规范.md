- 统一使用 Tailwind CSS 进行样式定义，普遍采用深色模式适配类名（如 `dark:bg-slate-900`）和响应式布局工具类。
- 在涉及外部链接跳转时，强制设置 `target="_blank"` 并配合 `rel="noopener noreferrer"` 以增强安全性。
- 日期格式化逻辑统一抽取为独立的辅助函数（如 `formatDate`），利用 `toLocaleDateString` 实现本地化显示。
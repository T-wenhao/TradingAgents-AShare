- 所有数据库写操作后必须显式调用 `db.commit()` 并可选调用 `db.refresh()` 以获取最新状态。
- 敏感信息（如 API Key、Webhook URL）在存入数据库前必须通过 `auth_service.encrypt_secret` 进行 Fernet 加密。
- 服务层函数普遍采用 `user_id` 作为数据隔离的核心过滤条件，确保多租户数据安全性。
- 批量操作或复杂逻辑中广泛使用 `uuid4().hex` 或 `str(uuid4())` 生成全局唯一标识符。
- 所有耗时或 I/O 密集型操作（如回测计算、邮件发送）均通过 `threading.Thread` 或 `asyncio.to_thread` 放入后台执行，避免阻塞主 API 线程。
- 数据库写操作遵循“查询-修改-提交-刷新”的标准 SQLAlchemy 模式，并在服务层统一处理 `db.commit()` 以确保事务原子性。
- 股票代码在处理前统一通过正则表达式标准化为 `XXXXXX.SH/SZ/BJ` 格式，确保跨模块数据一致性。
- 敏感配置（如 SMTP 密码、API Key）优先从环境变量读取，并在日志记录或返回前端时进行脱敏处理。
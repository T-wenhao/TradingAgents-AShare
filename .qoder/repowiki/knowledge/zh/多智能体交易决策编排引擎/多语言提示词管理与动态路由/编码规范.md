- 所有分析类提示词必须在末尾追加固定格式的机读摘要块 `<!-- VERDICT: ... -->`，用于程序化提取方向判断。
- 辩论与风控类提示词采用结构化状态追踪块（如 `<!-- DEBATE_STATE: ... -->`、`<!-- RISK_STATE: ... -->`），以 JSON 格式记录论据响应与未决事项。
- 提示词模板统一使用 Python f-string 风格的占位符（如 `{ticker}`、`{market_research_report}`）实现运行时动态内容注入。
- 中英文提示词字典保持键名严格一致，确保在语言切换时业务逻辑调用的稳定性。
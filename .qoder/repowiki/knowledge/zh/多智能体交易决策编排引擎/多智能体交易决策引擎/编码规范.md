- 所有智能体节点均采用工厂函数模式（如 create_trader），返回闭包函数以封装 LLM 实例与业务逻辑。
- 统一通过 current_tracker_var 获取追踪器实例，在 llm.astream 循环中实时捕获 Token 并推送到前端或日志系统。
- 辩论类节点遵循“结构化输出解析”约定，使用 HTML 注释标签（如 <!-- VERDICT: ... -->）包裹关键决策数据，并通过正则工具提取。
- 各节点在处理 Prompt 前均调用 memory.get_memories 检索历史相似情境，并将匹配到的建议注入上下文以增强决策连贯性。
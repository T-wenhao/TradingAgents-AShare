- 所有具体客户端类均继承自 `BaseLLMClient` 并强制实现 `get_llm` 和 `validate_model` 方法，确保接口一致性。
- 通过子类化 LangChain 聊天类并重写 `invoke` 等方法，在模块内部统一处理日志记录、内容清洗及参数预处理逻辑。
- 在 `get_llm` 方法中集中管理环境敏感配置（如 API Key 读取、Base URL 映射），并根据模型特征动态调整默认参数（如 Moonshot 模型强制 temperature=1）。